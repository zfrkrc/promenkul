version: '3'

services:
  webserver:
    image: nginx
    volumes:
      - ./nginx:/usr/share/nginx/html:ro
      - ./conf/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - 80:80
      - 443:443
    restart: always